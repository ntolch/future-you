<%= form_for(@new_situation) do |f| %>
  <% if @new_situation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_situation.errors.count, "error") %> prohibited this new_situation from being saved:</h2>

      <ul>
      <% @new_situation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
